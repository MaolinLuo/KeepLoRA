# Copy to ".env" in the same directory as the script (or export in your shell).
# Never commit real keys; ".env" is gitignored.

OPENAI_API_KEY=

# Optional API base URLs (older eval code uses openai.api_base / OpenAI client base_url).
# OPENAI_BASE_URL=https://orisound.cn/v1
# OPENAI_API_BASE=https://api.openai.com/v1
